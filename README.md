Простой набор скриптов для переноса треков из liked между разными аккаунтами SoundCloud.

Доступ к API soundcloud не дает, поэтому берем его сами.

Краткий гайд:

1. Зайти на сайт soundcloud.
2. Открыть консоль разработчика и вставить туда содержимое [`soundcloud.js`](./soundcloud.js).
3. Посмотреть количество треков в плейлисте `liked`.
4. Вызвать `dumpLikes(likedCount)`.
5. Скопировать результат.
6. Выполнить шаги 1 и 2 с другого аккаунта.
7. Вызвать `loadLikes(copiedLikes)`.
8. В случае возникновения проблем с 403 - обновить страницу и выполнить `continueLoads()`.

Проблемы:

1. Вставка содержимого завершается с ошибкой `appVersion or clientId parse failed. Call manualy parseClient(url)`:
   * Зайти во вкладку `Network`.
   * Скопировать url любого запроса.
   * Выполнить `parseClient(url)` вручную.
2. Лайк треков завершился с ошибкой `403`:
   * Сработала защита от спапа.
   * Необходимо ввести капчу и выполнить `continueLoads();`
3. Лайк треков завершился с ошибкой `429`:
   * Получен таймаут на лайки.
   * Необходимо выполнить `dumpUnliked();` и записать результат.
   * Далее, продолжить выполнение после того, как пройдет таймаут.
